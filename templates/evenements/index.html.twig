{% extends 'base.html.twig' %}

{% block body %}
{% block stylesheet %} {{ asset('css/evenements.css') }} {% endblock %}

<div class="wrapper">
    <div class="container">

{% for event in events %}
        <a href="event/{{ event.id }}">
            <div class="tile" style="background-image: url({{ asset(event.manifAffiche) }}) !important">
                <div class="type">{{ event.ManifGenre }}</div>
                <div class="text">
                    <p>{{ event.ManifDate }} - {{ event.ManifHoraire }}</p>
                    <h3>{{ event.ManifTitre }}</h3>
                </div>
            </div>
{% endfor %}
    </div>
</div>


{% endblock %}
