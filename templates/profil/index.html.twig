{% extends 'base.html.twig' %}
{% block title %}Profil{% endblock %}
{% block meta %}<meta name="robots" content="noindex,nofollow"/>{% endblock %}
{% block stylesheet %} {{ asset('css/profil.css') }} {% endblock %}

{% block body %}

<div class="wrapper">

    <h1>Bonjour {{ client.clientPrenom }} {{ client.clientNom }}</h1>

    <div id="infos">
        <div class="container" id="info">
            <h2>Vos Informations</h2>
            <div class="data">
                <p>Email : {{ client.email }}</p>
                <p>Prénom : {{ client.clientPrenom }}</p>
                <p>Nom : {{ client.clientNom }}</p>
            </div>
            <a href="{{ path('app_client_edit', {'id': client.id}) }}" class="button">Modifier</a>
        </div>

        <div class="container" id="adr">
            <h2>Votre adresse</h2>
            <div class="data">
                <p>Adresse : {{ client.clientAdrRue }}</p>
                <p>Ville : {{ client.ClientAdrVille }}</p>
                <p>Code postal : {{ client.ClientAdrCp}}</p>
            </div>
            <a href="{{ path('app_client_edit_adresse', {'id': client.id}) }}" class="button">Modifier</a>
        </div>

        <div class="container">
            <h2>Vos commandes</h2>
            <div class="data">
                <div class="commande">
                    <a href="#">
                        <p href="">Commande 1</p>
                        <div class="control">
                            <a href="#"><img src="{{ asset('svg/voir.svg') }}" alt="Voir commande"></a>
                            <a href="#"><img src="{{ asset('svg/file.svg') }}" alt="Voir billet"></a>
                        </div>
                    </a>
                </div>

                <div class="commande">
                    <a href="#">
                        <p href="">Commande 1</p>
                        <div class="control">
                            <a href="#"><img src="{{ asset('svg/voir.svg') }}" alt="Voir commande"></a>
                            <a href="#"><img src="{{ asset('svg/file.svg') }}" alt="Voir billet"></a>
                        </div>
                    </a>
                </div>

                <div class="commande">
                    <a href="#">
                        <p href="">Commande 1</p>
                        <div class="control">
                            <a href="#"><img src="{{ asset('svg/voir.svg') }}" alt="Voir commande"></a>
                            <a href="#"><img src="{{ asset('svg/file.svg') }}" alt="Voir billet"></a>
                        </div>
                    </a>
                </div>
            </div>
            <a href="{{ path('app_client_commandes') }}" class="button">Voir plus</a>
        </div>
    </div>
</div>

{% endblock %}